{
  "name": "sls-appsync-backend",
  "version": "1.1.0",
  "description": "An example backend for AppSync with Amplify outputs",
  "main": "handler.js",
  "scripts": {
    "deploy": "./node_modules/.bin/sls deploy -v"
  },
  "license": "MIT",
  "devDependencies": {
    "aws-amplify-serverless-plugin": "file:..",
    "chalk": "^2.4.1",
    "serverless": "^1.32.0",
    "serverless-appsync-plugin": "^1.0.5"
  }
}
